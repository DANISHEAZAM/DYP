<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="danish.azam.dyp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signupactivity" />
        <activity android:name=".picactivity" />
        <activity android:name=".feestructure" />
        <activity android:name=".resetactivity" />
        <activity android:name=".galleryactivity" />
        <activity android:name=".AFactivity" />
        <activity android:name=".finalactivity" />
        <activity android:name=".marksactivity" />
        <activity android:name=".superfinalactivity" />
        <activity android:name=".theendactivity" />
        <activity android:name=".contactus"></activity>
    </application>

</manifest>